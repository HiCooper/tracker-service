tracker:
  session:
    timeout-minutes: 30
    cleanup-interval-ms: 60000
  dedup:
    two-stage-enabled: true
  dlq:
    replay-interval-ms: 10000
    replay-initial-delay-ms: 5000

logging:
  level:
    com.gateflow.tracker: DEBUG
