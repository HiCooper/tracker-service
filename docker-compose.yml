# ============================================
# Tracker Service - use shared infrastructure
#
# Local dev: run via IDE, connect to shared infra (localhost ports)
#   Start shared infra first:
#     cd ../victor-ab && docker compose -f docker/docker-compose.infra.yml up -d
# ============================================

networks:
  shared-infra:
    external: true
